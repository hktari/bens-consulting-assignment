# Introduction

This assignment implements a product management app.

On the frontend it uses:
- angular
- tailwindCSS
- ng-icons

On the backend it uses:
- json-server
- a script to generate `db.json` from the json files inside `data` folder

# How to run

1. `npm run json-server`
2. `npm run start`